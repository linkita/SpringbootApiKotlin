name: A workflow for cdsc-outbound
on: push
jobs:
  build:
    name: test action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Execute tests
        run: docker-compose run outbound-api
      - name: "ktlint"
        uses: "vroy/gha-kotlin-linter@v1"